# Metodologia

Pendiente. Este documento se llena a medida que se desarrolla cada tema: que modelos matematicos o fisicos se usaron, que supuestos y simplificaciones se tomaron (ej. por que un canal AWGN simplificado y no un modelo de canal mas complejo), y por que.

Dado que el modulo tiene 21 temas independientes (ver README.md), este documento puede organizarse como una seccion por tema a medida que cada uno se integre, en lugar de escribirse todo de una vez.
